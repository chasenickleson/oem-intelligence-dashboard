Upload the scripts folder over the existing scripts folder in the repository. This package intentionally does not replace index.html, dashboard-data.json, or the workflow, so your scroll-to-title navigation, scope subtitles, current data, and corrected GitHub workflow remain unchanged. After upload, run Refresh dashboard data once. The workflow will rewrite the existing JSON with fully segment-specific summaries, briefs, outlooks, economics and opportunities.
